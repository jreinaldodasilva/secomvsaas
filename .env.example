# Required — URL of the backend API (e.g. http://localhost:5000)
VITE_API_URL=http://localhost:5000

# Optional — deployment environment (development | staging | production). Defaults to 'development'.
VITE_APP_ENV=development
